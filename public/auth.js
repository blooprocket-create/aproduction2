
const API={async _parseJSON(r){const t=await r.text();try{return JSON.parse(t)}catch{throw new Error(`API returned non-JSON (status ${r.status}). First bytes: ${t.slice(0,120)}`)}},async get(p,a=!1){const r=await fetch(`/api/${p}`,{headers:{...(a?authHeader():{})}});return this._parseJSON(r)},async post(p,d,a=!1){const r=await fetch(`/api/${p}`,{method:'POST',headers:{'Content-Type':'application/json',...(a?authHeader():{})},body:JSON.stringify(d||{})});return this._parseJSON(r)}};function currentUser(){try{return JSON.parse(localStorage.getItem('novus:user')||'null')}catch{return null}}function logoutUser(){localStorage.removeItem('novus:token');localStorage.removeItem('novus:user');location.href='index.html'}function authHeader(){const t=localStorage.getItem('novus:token');return t?{Authorization:'Bearer '+t}:{}}function initNav(){const u=currentUser();const nav=document.querySelector('nav.flex');if(!nav)return;nav.innerHTML=`
  <a href="catalog.html">Courses</a>
  <a id="accountLink" href="account.html" style="display:none">Account</a>
  <a id="adminLink" href="admin.html" class="btn secondary" style="display:none">Control Panel</a>
  <a id="loginLink" href="login.html">Login</a>`;const login=document.getElementById('loginLink'),acct=document.getElementById('accountLink'),admin=document.getElementById('adminLink');if(u){acct.style.display='';login.textContent=`Logout (${u.name||'Account'})`;login.href='#';login.onclick=e=>{e.preventDefault();logoutUser()};if(u.role==='admin'||u.role==='editor')admin.style.display=''}}document.addEventListener('DOMContentLoaded',initNav);
